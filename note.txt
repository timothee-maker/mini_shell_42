Tests du parsing :
- gerer les quotes
- : \ () & ; et ! sont pas geres
- affiche pas les erreurs de syntaxe
- \ ne nullifie pas $ par ex
- remplacer les env var meme collees aux autres mots du parsing
- traduire les env var doit etre passe au parsing (cf test 328 du gdoc)

Tests de l'exec :
- CTRL+C interrompt pas toutes les cmd
- CTRL+D fonctionne pas si ligne existante
- CTRL+\ = zsh: quit (core dumped)  ./minishell
- CTRL+Z = zsh: suspended  ./minishell
- export : revoir les var exportees et gere le cas ou pas de valeur
- export : check valid identifier (char speciaux, espaces ...)
- cd renvoie bad adress si 0 arg (doit renvoyer a HOME)
- revoir messages d'erreur sur cd
- revoir messages d'erreur sur pwd
- revoir l'execution des fichiers
- gerer le too many arguments sur exit
- revoir fonctionnement d'exit dans les pipe
